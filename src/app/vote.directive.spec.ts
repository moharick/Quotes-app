import { VoteDirective } from './vote.directive';

describe('VoteDirective', () => {
  it('should create an instance', () => {
    const directive = new VoteDirective();
    expect(directive).toBeTruthy();
  });
});
